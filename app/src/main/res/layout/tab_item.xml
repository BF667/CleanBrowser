<?xml version="1.0" encoding="utf-8"?>
<!-- Chrome-style tab card: rounded rectangle with thumbnail preview area + title -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tab_card_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/tab_card"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp"
        app:cardBackgroundColor="#1E1E32"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Group color strip (top, 4dp, visible when tab is in a group) -->
            <View
                android:id="@+id/tab_group_strip"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:background="#4285F4"
                android:visibility="gone" />

            <!-- Thumbnail preview area -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="#13132A">

                <!-- Actual thumbnail image -->
                <ImageView
                    android:id="@+id/tab_thumbnail"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:contentDescription="Preview" />

                <!-- Favicon + domain overlay (bottom-left) -->
                <LinearLayout
                    android:id="@+id/tab_favicon_container"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|start"
                    android:layout_margin="8dp"
                    android:background="#CC000000"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/tab_favicon"
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:gravity="center"
                        android:textColor="#FFFFFF"
                        android:textSize="10sp"
                        android:fontFamily="sans-serif-medium" />

                    <TextView
                        android:id="@+id/tab_domain"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="6dp"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:textColor="#B0B0B0"
                        android:textSize="11sp"
                        android:fontFamily="sans-serif" />
                </LinearLayout>

                <!-- Incognito badge (bottom-right) -->
                <ImageView
                    android:id="@+id/tab_incognito_badge"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="bottom|end"
                    android:layout_margin="8dp"
                    android:src="@drawable/ic_incognito"
                    android:tint="#9CA3AF"
                    android:visibility="gone"
                    android:contentDescription="Incognito" />

                <!-- Close button (top-right) -->
                <ImageView
                    android:id="@+id/tab_close"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="end|top"
                    android:layout_margin="6dp"
                    android:background="@drawable/tab_close_bg"
                    android:padding="6dp"
                    android:src="@drawable/ic_close"
                    android:tint="#B0B0B0"
                    android:contentDescription="Close tab" />

            </FrameLayout>

            <!-- Title bar below thumbnail -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="36dp"
                android:gravity="center_vertical"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:orientation="horizontal">

                <!-- Group label (if in group) -->
                <View
                    android:id="@+id/tab_group_dot"
                    android:layout_width="6dp"
                    android:layout_height="6dp"
                    android:background="#4285F4"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/tab_group_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:layout_marginEnd="6dp"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textColor="#6B7280"
                    android:textSize="10sp"
                    android:fontFamily="sans-serif"
                    android:visibility="gone" />

                <!-- Page title -->
                <TextView
                    android:id="@+id/tab_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textColor="#D1D5DB"
                    android:textSize="12sp"
                    android:fontFamily="sans-serif" />

            </LinearLayout>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</FrameLayout>